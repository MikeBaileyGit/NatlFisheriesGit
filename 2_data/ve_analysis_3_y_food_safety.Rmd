---
title: "Model 3: Food Safety Refusals Explained by Base Specification"
author: "Vince Egalla"
date: "12/27/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
options(digits = 3)
options(scipen=999)
```

## Dependencies

```{r}
library(ggplot2)
library(lmtest)
```

## Load Data
```{r}
## Combined FDA + USDA data
load("output/build/ve_analysis_dataset_2.Rdata")
```



## Linear Regression
```{r}
## Base Model
ols.3 = lm(fs_refusals ~  USDA01 + USDA_transition01 + import_volume_1M + USDAxVolume1M +  factor(month) + factor(country), data= monthly)

summary(ols.3)

save(ols.3, file="output/analysis/ve_analysis_3_ols.RData")

coeftest(ols.3, vcov = vcovHC(ols.3, type="HC1"))

sigma(ols.3)
```
```{r}
## Base Model
ols.3.no = lm(fs_refusals ~  USDA01 + USDA_transition01 + import_volume_1M + USDAxVolume1M +  factor(month) + factor(country), data= monthly[monthly$yrmonth != as.yearmon("Sep 2017"),])

summary(ols.3.no)
```